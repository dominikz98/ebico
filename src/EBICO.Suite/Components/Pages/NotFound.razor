@page "/not-found"
@layout MainLayout

@* Reached via UseStatusCodePagesWithReExecute („/not-found") and the Router's NotFoundPage. German like
   the rest of the shell, with an h1 so <FocusOnNavigate Selector="h1" /> in Routes.razor finds a target
   and screen readers get a page heading (issue #126). *@

<PageTitle>Seite nicht gefunden</PageTitle>

<h1>Seite nicht gefunden</h1>

<p class="text-muted">
    Die aufgerufene Adresse gehört zu keiner Seite dieser Oberfläche.
</p>

<p>
    <a href="">Zurück zur Übersicht</a>
</p>
