@page "/stammdaten"

<PageTitle>Stammdaten</PageTitle>

<h1>Stammdaten</h1>

<p class="text-muted">
    Verwaltung der registrierten Banken, Partner (Kunden) und Teilnehmer des Emulators:
    Anlegen, Bearbeiten, Löschen sowie Teilnehmer-Status und Berechtigungen.
</p>

@* Static-SSR-Seite mit interaktiven Inseln je Aggregat (ADR-0009: Render-Modus am Einbettungsort).
   Die Komponenten schreiben in-process über den IMasterDataManager (EBICO.Server). *@
<BankManager @rendermode="InteractiveServer" />
<PartnerManager @rendermode="InteractiveServer" />
<SubscriberManager @rendermode="InteractiveServer" />
