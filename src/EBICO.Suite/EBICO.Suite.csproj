<Project Sdk="Microsoft.NET.Sdk.Web">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <BlazorDisableThrowNavigationException>true</BlazorDisableThrowNavigationException>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\EBICO.Core\EBICO.Core.csproj" />
    <!-- In-process access to the server state (store + master-data manager) per ADR-0009:
         the Suite calls IMasterDataManager directly instead of via its own HTTP API. -->
    <ProjectReference Include="..\EBICO.Server\EBICO.Server.csproj" />
  </ItemGroup>

</Project>
